# 2026-04-08 Reflection

Today I moved from basic input handling into control flow and conditionals.

The key shift was understanding that programs don’t just process data—they make decisions based on that data. Using if/elif/else allowed me to build a simple age classifier, which helped reinforce how logic flows in a program.

I also encountered and fixed an error related to applying .strip() after type conversion, which reinforced the importance of ordering operations correctly (clean input before converting types).

This feels like a transition point from writing simple scripts to building logic-driven programs.